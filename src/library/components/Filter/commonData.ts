import { SxProps, Theme } from '@mui/material';

// eslint-disable-next-line import/prefer-default-export
export const filterLabelSx: SxProps<Theme> = ({ palette }) => ({
	color: palette.common.white,
	fontWeight: 700,
});
